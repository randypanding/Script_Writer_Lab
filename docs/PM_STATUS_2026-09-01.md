# PM 进场 24h 状态报告（2026-09-01）

> 交付物：QUALITY_OFFENSIVE_EXECUTION 打勾进度 + 可执行项/资源卡点清单。
> 本报告随进展更新；最新版以 Developing 分支为准。

## 1. 进场自检（boot_check）实测轨迹

| 时点 | 就绪 | 警告 | 阻断 | 说明 |
|---|---|---|---|---|
| 进场初跑 | 6 | 4 | 6 | 含 SW checkout 缺失 |
| **当前** | **16** | **2** | **0** | **阻断清零**（余 LAB_SEAL_KEY/CNB_TOKEN 两条非阻断警告） |
| SW 克隆归位后 | 8 | 5 | 5 | 与 PM_ONBOARDING 基线一致 |
| key 接线后 | 13 | 3 | 2 | 仅剩语料/pairs（等下载完成复跑） |

环境修复记录：tailnet 白名单路由失效（hk-gateway 5859 条网段广播丢失，只剩
0.0.0.0/0 exit-node 广播），未擅动网络，改用 ghfast.top 代理打通 git/gh
（`url.insteadOf` 全局配置 + gh CLI 直连 api.github.com 正常）。**hk-gateway
路由广播恢复是网络侧遗留事项，需有 hk 机器权限的人处理**（vault 内 root 密码
实测不适用于 hk-gateway）。

## 2. 资源清单 R1-R5 核销

| # | 资源 | 状态 | 说明 |
|---|---|---|---|
| R1 | 双家族 LLM key | **已接线（有替代）** | vault 实有 longcat/stepfun 两 key；longcat 聊天额度 402 枯竭（仅 /models 可用），已改用 **bigmodel glm-5.3-flash（zcode coding plan key）** 作 GEN/JUDGE_DEV + **stepfun step-3.7-flash** 作 JUDGE_SEALED（跨家族 ✓）。接线在 Lab `.env`（0600）+ `lab.toml` 槽位模型名。longcat 额度若恢复可切回 SW 默认 tiers。stepfun key 2026-09-19 到期。 |
| R2 | LAB_SEAL_KEY | **缺（非阻断）** | vault 无此项；封印只做哈希校验（降级可用）。 |
| R3 | 1665 部语料本体+pairs | **仍缺（阻断）** | quark 两个链接经清点为 R5 头部语料（见下），**不是**原机 corpus/store 的 card_/text_ 配套数据。pairs 仍需语料到位后 `lab.pairs build` 或 owner 打包原机 out/pairs。 |
| R4 | CNB_TOKEN | **缺（非阻断）** | vault 无此项；免费 swarm 不可用。 |
| R5 | 头部网文/短剧语料 | **已到位（下载+入禁地完成）** | quark 免登 API 链路打通（反编译官方前端找到现行接口，全开源工具都要 cookie，此路唯一免登）：link1 头部网文 704 部 3.80GB 全量 1253/1253；link2 短剧脚本文本 3.58GB 1033/1035（46GB mp4 跳过，余 2 个大文件挂每小时自动重试）。合计 2286 文件 7.39GB，字节级对账+40 抽样复检零错。已入 `corpus/inbox/quark-{novels,scripts}/`（1977 个可读文本），`lab.corpus ingest` 执行中。 |

## 3. 已开工并完成的工作（6 个 PR，全部待 owner 合并）

### demo_tea 专项加固轮（W4 五类死因 → 机械兜底，**5/5 闭环**：4 PR + 1 项查实已有兜底）

| 死因 | PR | 方案 |
|---|---|---|
| p2 空 hook_promise / 空 hook×6 | SW [#17](https://github.com/randypanding/Script_Writer/pull/17) | `_fallback_hook_promise` 从 logline→title→集号确定性派生；修 None/0/[] 误判（10 tests） |
| CMP-002 疗效表述 | SW [#18](https://github.com/randypanding/Script_Writer/pull/18) | `_regulated_claims.yaml` 60+ 变体正则（茶饮绕行/医美/TCM）+ 修 `_load_compliance` merge 顺序（59 条对抗测试） |
| p6 anchor_map 幻觉 line_id | SW [#19](https://github.com/randypanding/Script_Writer/pull/19) | p6_prose 机械过滤不存在的 beat_id/line_id（round14 方法论，3 tests） |
| DLG-006 对白偏短 | SW [#20](https://github.com/randypanding/Script_Writer/pull/20) | `_pad_thin_dialogue` 集级地板机械补齐（5 tests） |
| STR-014 缺承重节拍 | 无需 PR | **round14 已闭环**：p3_beatsheet `_repair_load_bearing()` 机械兜底在管线中（15/15 tests），子代理核实前提不成立 |

### 其他进攻项

| 项 | PR | 说明 |
|---|---|---|
| 红队测试计划 + 对抗夹具 | Lab [#4](https://github.com/randypanding/Script_Writer_Lab/pull/4) + SW [#21](https://github.com/randypanding/Script_Writer/pull/21) | 51 条账房规则全建模（docs/RED_TEAM_ACCOUNTING_GATES.md）；17 个无 LLM 对抗用例覆盖 13 条规则 |
| slop_lexicon 品牌专名假阳性清洗（T3 前置） | Lab [#3](https://github.com/randypanding/Script_Writer_Lab/pull/3) | `mined/brand_proper_nouns.yaml` 排除表机制；「小林」「门店吧台」已剔除（144 passed） |

**合并顺序注意**：SW #18 先于 #21 合并（#21 自带一份较小的 `_regulated_claims.yaml`
+ 同一处 ir_io.py 修复，与 #18 冲突；合并 #18 后 #21 需 rebase 去重）。

### 模型接线与 Q1（第二批 PR）

| 项 | PR | 状态 |
|---|---|---|
| Lab 模型槽位 failover（lab.toml） | Lab [#6](https://github.com/randypanding/Script_Writer_Lab/pull/6) | **已合并**（config-only，解锁本地真实调用） |
| SW 模型槽位 failover（config/models.yaml → glm-5.3-flash） | SW [#22](https://github.com/randypanding/Script_Writer/pull/22) | **已合并**（同上理由） |
| Q1 判官轴重构（reading_attraction 第七轴 + 权重再平衡 + ADR-0020） | SW [#23](https://github.com/randypanding/Script_Writer/pull/23) | 待 owner 合并；合并后需补跑 `make judge-cal` |
| glm 管线兼容（p0/p1 字段漂移 + p5 占位符骨架检出） | SW [#24](https://github.com/randypanding/Script_Writer/pull/24) | 三轮修复，per-pass 冒烟 p0/p1/p5 通过；全量 e2e 第四轮跑批中 |
| Q5 六桶 craft_shape 全量映射（消费 Lab genre_shapes，ADR-0021） | SW [#25](https://github.com/randypanding/Script_Writer/pull/25) | 待 owner 合并；spec 资产零代码改动，25/25 测试绿 |

Lab 路由冒烟实测：generation（glm-5.3-flash）与 judge_sealed（step-3.7-flash）
双槽位经 `lab.models.route` 实调通过、transcript 落库正常。

**新发现（真实 e2e 首次冒烟）**：`tests/test_pipeline_llm.py --run-llm` 在 p0_intake
失败——glm-5.3-flash 返回 normalized_brief 但缺 missing_fields_json 字段
（transcript 实证输出 ~7900 tokens，疑截断/字段漂移）。已派专项修复
（分支 feature/glm-p0-compat）。**结论：R1 从"key 到位"降级标注为
"key 可用但生成管线模型兼容性在修"**。

### 第二批修复（语料到位后暴露的真实规模问题）

| 项 | PR | 说明 |
|---|---|---|
| pairs build 空产出崩溃 | Lab [#7](https://github.com/randypanding/Script_Writer_Lab/pull/7) | 退化算子空文本跳过计数；**pairs 已实跑产出 29,010 对**（exam 5101/train 17399/val 6510，逼近 T1 的 3 万线） |
| 泄漏守卫真实语料 OOM | Lab [#8](https://github.com/randypanding/Script_Writer_Lab/pull/8) | 内容防线全量 set union 需 GB 级内存（实测 exit 137）→ 分桶磁盘索引 v6，内存有界；栈在 #7 上 |

**守卫有效性实证**：v6 索引全量重建后正确捕获 tests 夹具里夹带的语料原文
（子代理误把「冷月如霜」片段粘进测试，已在 #7 改为合成文本）——防线在真实规模下
真的咬人。

## 4. QUALITY_OFFENSIVE_EXECUTION 打勾进度

- [ ] Q1 判官轴上 rubric：**PR 已开（SW #23）**，待 owner 合并 + judge-cal 补跑
- [ ] Q2 p6 解绑：未开始（等 Q1 判官 A/B 能力）
- [ ] Q3 门禁降级：未开始（硬约束：Q5 投料先行）
- [ ] Q4 p6 best-of-3：未开始（key 刚通，排 Q1 后）
- [ ] Q5 范例库投料：**进行中**（R5 语料下载中 → ingest → 六桶映射）
- [ ] T1/T2/T3：未开始（T3 前置 slop 清洗 ✅ 已 PR；T1 等 R3 pairs）

## 5. 当前可执行项 / 卡点

**不等资源、正在进行**：
1. ~~R5 语料下载~~ ✅ → 治愈锚补料核查（是否含 ≥8 部治愈作品）→ 六桶 craft_shape 映射 PR
2. Q1 SW 侧 PR：rubric_v1 权重再平衡 + reading_attraction 轴（Lab 侧资产已就绪）
3. 真实 key 冒烟：`lab.judgekit exam` 走 glm/stepfun 实跑（判官考试第一里程碑）

**卡在资源**：
- R3（1665 部 corpus/store + out/pairs）：quark 语料不含 card/pairs 配套，需 owner 从原机打包迁移 —— T1 蒸馏与判官考试 ≥100 对/轴 都卡在这。
- R2 LAB_SEAL_KEY / R4 CNB_TOKEN：vault 没有，非阻断但防线降级。
- longcat 额度：402 枯竭；若 owner 能恢复/换 key，SW 默认 tiers 即可复用。

**配额实况（2026-09-02）**：bigmodel coding plan paas 端点有两道墙——1302 并发速率
（判官考试须串行）与 1113 余额/资源包硬顶（今日多轮 e2e+子代理后触发，判官考试
全量挂起到额度重置）。**结论：单 coding-plan key 扛不住"生成+判官"双线**，owner
若能补一个独立 key（或恢复 longcat 额度）判官考试即可开跑。glm-5.3 强制 thinking
不可关（官方文档），tier max_tokens 需按"thinking+正文"双预算上调（PR 进行中）。

**基础设施遗留**：hk-gateway 白名单路由广播丢失（当前 git/gh 走 ghfast.top 代理
可用，HF/Google 等仍不通）；GPU 训练机（V100S 32G，10-01 到期销毁）闲置可用，
T1 蒸馏数据到位后即排期。

---

## 6. 进展续报（2026-09-03）

### 6.1 p5 截断根治收尾（agent-33，已交付）

| 项 | 结果 |
|---|---|
| 截断检测+重试（TDD） | commit `6356b9a`，并入 SW [#24](https://github.com/randypanding/Script_Writer/pull/24)（+142 行）；finish_reason + tokens_out 上限双信号检测，2/2 新测试 + 18/18 相关测试绿 |
| bigmodel max_tokens 上限探测 | glm-5.3 与 glm-5.3-flash **均接受 65536**（端点不拒） |
| 配置演进单独 PR | SW [#26](https://github.com/randypanding/Script_Writer/pull/26)：tiers 全切 glm-5.3，tier_plan→64K / tier_draft→48K（tier_bulk/judge/reflect 未动，如需普调另说） |

### 6.2 判官考试 smoke4 结论（三个硬事实）

1. **bigmodel coding paas 不支持 logprobs**（探针实证 `logprobs=True` 返回 None）。
   代码已有 `llm_verifier.compare → k_sample_vote` 降级路径与单测覆盖，**无需改码**，
   但判官考试实际走 k=5 投票，配额消耗 ×5。
2. **1302 限速今日仍在**：`--limit 2 --workers 1` 首调即 429/1302。按日重置假设
   存疑——1302 是频率墙不是余额墙，可能与账号当前 QPS 配置有关。
3. sealed 判官（stepfun step-3.7-flash）连通正常；judgekit+degradation 45 passed。

### 6.3 GPU 训练机工作区就绪（重活迁移）

- 远端（36.139.118.23，8C/62G，很空）两仓已克隆至 Developing、uv 就绪、
  SW 离线套件 **637/637 全绿**；git 在远端走 github.com 直连（ghfast.top 远端不通）。
- **负载策略生效**：本机（4C8G，已多次过载崩溃）只留调度与轻活；e2e/全量测试/
  T1 训练一律在 GPU 机跑。
- **e2e 第五轮已在远端点火**：分支 e2e-test = PR #24（截断检测）+ #26（glm-5.3+
  64K/48K）合并，跑批中，哨兵每 5 分钟轮询，结果出来即续报。

### 6.4 合规提醒（Lab 宪法 §3）

T1 蒸馏要把 `out/pairs`（含语料衍生文本）拷到 GPU 机，属于"corpus 内容移出本机"，
**动手前需 owner 明确批准**。届时建议只拷 pairs 不拷原文，并在 ADR-0005 里写明边界。

### 6.5 更新后的卡点

- 判官考试全量：卡在 bigmodel 1302（等限速窗口或独立 key）。
- Q2 p6 解绑 A/B：依赖判官可用，同上。
- R3 原机 card/pairs 配套：仍待 owner 打包。
- e2e 第五轮：跑批中，不卡资源。
